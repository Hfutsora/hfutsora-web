const getters = {
  navHeight: state => state.constant.navHeight,
  nim: state => state.im.nim,
  messageList: state => state.im.messageList,
  messageListLength: state => state.im.messageListLength,
};

export default getters;
